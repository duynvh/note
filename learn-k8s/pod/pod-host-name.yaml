apiVersion: v1
kind: Pod
metadata:
  name: pod-host-name
  # namespace: frontend
  labels:
    app: my-app
spec:
  hostname: my-nginx
  subdomain: my-nginx-1
  containers:
    - name: nginx-container
      image: nginx
      ports:
        - containerPort: 80
